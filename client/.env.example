# Backend port (must match uvicorn --port)
VITE_API_PORT=8000

# Optional: full API URL instead of Vite proxy
# VITE_API_URL=http://localhost:8000/api

# Voice generator uses /api/tts/* via the same proxy as inpainting
