{
  "name": "clearmark",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "npm run dev --prefix client",
    "build": "npm run build --prefix client",
    "server": "cd server && .venv/bin/python -m uvicorn main:app --reload --port 8000"
  }
}
